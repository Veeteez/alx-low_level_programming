#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 *
 * Return: Always O
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar(letter);
	putchar('\n');
	Return(0);
}
