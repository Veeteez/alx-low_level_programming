#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 *
 * Return: Always O
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
		putchar(c);
		c++;
		}
	putchar('\n');
	Return(0);
}
